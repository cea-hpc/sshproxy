dist: xenial

language: go

# disable shallow clone
git:
  depth: false

services:
  - docker

matrix:
  include:
    - go: 1.10.x
    - go: tip
  allow_failures:
    - go: tip

notifications:
  email: false

script: make exe && make test
